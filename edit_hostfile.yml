- hosts: 127.0.0.1
  become: yes
  vars_files:
    - vars/vmware_vms.yml
  
  tasks:
  - name: Update /etc/hosts from inventory
    lineinfile:
      path: /etc/hosts
      line: "{{ item }}"
      state: present
    with_items: "{{ vmware_smartlis_deployment.keys() }}"
