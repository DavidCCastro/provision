- hosts: "{{ servers }}"
  gather_facts: no

  tasks:
  - name: Get disk facts
    win_disk_facts:

  - name: Output first disk space
    debug:
      var: ansible_facts.disks[1].partitions[1].volumes
