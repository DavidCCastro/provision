- hosts: "{{ servers }}"
  gather_facts: false
  
  tasks:
  - name: Check smartlis web
    uri:
      url: http://{{ smartlis_ip }}/system/about
    register: output
  
  - name: Debug output
    debug:
      var: output.json
