- hosts: "{{ servers }}"
    
  tasks:
  - name: Check supervisor status
    become: yes
    shell: supervisorctl status
    register: superv_status
    
  - name: debug
    debug:
      var: superv_status
